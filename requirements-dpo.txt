# Optional dependencies for Unsloth fine-tuning scripts.
# Keep separate from the base runtime because these packages are CUDA/TRL-version sensitive.

unsloth
trl
peft
bitsandbytes>=0.46.0
accelerate>=0.33.0
wandb>=0.18.0,<0.25
huggingface_hub>=0.24.0
mergekit
