# Runtime dependencies for scraping, Hugging Face dataset export, and tokenizer utilities.
# pymahjong is intentionally excluded here because it is installed via ./scripts/setup_pymahjong.sh.

datasets==4.6.1
numpy==2.4.2
protobuf==7.34.0
requests==2.32.5
tokenizers==0.22.2
tqdm==4.67.3
transformers==5.3.0
